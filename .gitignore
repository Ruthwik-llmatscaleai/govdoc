# dependencies
node_modules/

# next.js build output
.next/
out/
build/
dist/
*.tsbuildinfo

# env / secrets
.env
.env.local
.env.*.local
.env.local.*
.env*.bak

# editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# testing (not needed for deployment)
coverage/
playwright-report/
test-results/
tests/
vitest.config.ts
playwright.config.ts
**/*.test.ts
**/*.test.tsx
**/*.spec.ts
**/*.spec.tsx

# private fixtures (PII-bearing real PDFs — never committed)
tests/fixtures-private/

# Sunnyvale Finance KB source PDFs + generated vision cache (provisioned at ingest time, not in repo)
finance/*.pdf
finance/.vision-cache/

# misc
*.log
.cache/

# git worktrees (project-local, per superpowers:using-git-worktrees skill)
.worktrees/

# AI agent configs (project-local instructions, not for the repo)
CLAUDE.md
GEMINI.md
AGENTS.md
.cursorrules
.aider*
.continue/
.claude/

# Internal planning docs (superpowers workflow plan artifacts, not for the repo)
docs/superpowers/

# Per-project CUCP institutional-memory precedents (created at runtime, not in repo).
artifacts/precedents/

# Per-usecase rubric edits saved via /work/search/edit (created at runtime, not in repo).
artifacts/rubrics/
.superpowers/
.designs/
service_account.json
